<apex:page controller="searchaccount">
   
    <apex:pageBlock title="account with a name">
    <apex:pageBlockTable value="{!a}" var="item">
    <apex:column value="{!item.id}"/>
    <apex:column value="{!item.Name}"/>
</apex:pageBlockTable>
   </apex:pageBlock>

   </apex:page>